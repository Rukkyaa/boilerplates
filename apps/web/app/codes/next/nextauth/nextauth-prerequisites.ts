export const nextAuthPrerequisites = `- Packages to install: @types/bcryptjs, bcryptjs, next-auth
- Do not put the middleware.ts inside the app folder, but in the root folder
- Add NEXTAUTH_SECRET="<your secret>" NEXTAUTH_URL="http://localhost:3000" (replace with your url) to the .env file
`;
